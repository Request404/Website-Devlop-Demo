server:
  port: 8081


spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/request404?serverTimezone=UTC

  redis:
    host: #redis 地址
    password: #redis 密码

springfox:
  documentation:
    swagger-ui:
      enabled: on

jwt:
  # 密匙Key
  secret: Request404@Jwt
  # 配置白名单（不需要认证）
  antMatchers: /user/account/**, /static/**